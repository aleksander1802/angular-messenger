export enum ProfileActionType {
    PROFILE_LOAD = '[Profile] Load Profile',
    PROFILE_LOAD_SUCCESS = '[Profile] Load Profile Success',
    PROFILE_LOAD_FAILURE = '[Profile] Load Profile Failure',
}
